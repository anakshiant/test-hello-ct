# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_modular_headers!

target 'cttest' do
    platform :ios, '12.0'
    pod 'CleverTap-iOS-SDK', '~> 3.10.0'
end